// ------- Expand line 1 ------- :
// Expand line 1 - toggle 1:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand1') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle1').onclick = toggle;

// Expand line 1 - toggle 2:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand1') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle2').onclick = toggle;

// Expand line 1 - toggle 3:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand1') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle3').onclick = toggle;

// Expand line 1 - toggle 4:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand1') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle4').onclick = toggle;



// ------- Expand line 2 ------- :
// Expand line 2 - toggle 1.1:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand2') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle1.1').onclick = toggle;

// Expand line 2 - toggle 2:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand2') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle2.1').onclick = toggle;

// Expand line 2 - toggle 3:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand2') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle3.1').onclick = toggle;

// Expand line 2 - toggle 4:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand2') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle4.1').onclick = toggle;




// ------- Expand line 3 ------- :
// Expand line 3 - toggle 1.1:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand3') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle1.2').onclick = toggle;

// Expand line 3 - toggle 2:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand3') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle2.2').onclick = toggle;

// Expand line 3 - toggle 3:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand3') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle3.2').onclick = toggle;

// Expand line 3 - toggle 4:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand3') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle4.2').onclick = toggle;




// ------- Expand line 4 ------- :
// Expand line 4 - toggle 1:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand4') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle1.3').onclick = toggle;

// Expand line 4 - toggle 2:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand4') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle2.3').onclick = toggle;

// Expand line 4 - toggle 3:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand4') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle3.3').onclick = toggle;

// Expand line 4 - toggle 4:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand4') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle4.3').onclick = toggle;




// ------- Expand line 1 ------- :
// Expand line 5 - toggle 1:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand5') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle1.4').onclick = toggle;

// Expand line 5 - toggle 2:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand5') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle2.4').onclick = toggle;

// Expand line 5 - toggle 3:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand5') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle3.4').onclick = toggle;

// Expand line 5 - toggle 4:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand5') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle4.4').onclick = toggle;




// ------- Expand line 1 ------- :
// Expand line 6 - toggle 1:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand6') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle1.5').onclick = toggle;

// Expand line 6 - toggle 2:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand6') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle2.5').onclick = toggle;

// Expand line 6 - toggle 3:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand6') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle3.5').onclick = toggle;

// Expand line 6 - toggle 4:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand6') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle4.5').onclick = toggle;




// ------- Expand line 7 ------- :
// Expand line 7 - toggle 1:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand7') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle1.6').onclick = toggle;

// Expand line 7 - toggle 2:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand7') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle2.6').onclick = toggle;

// Expand line 7 - toggle 3:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand7') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle3.6').onclick = toggle;

// Expand line 7 - toggle 4:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand7') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle4.6').onclick = toggle;




// ------- Expand line 8 ------- :
// Expand line 8 - toggle 1:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand8') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle1.7').onclick = toggle;

// Expand line 8 - toggle 2:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand8') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle2.7').onclick = toggle;

// Expand line 8 - toggle 3:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand8') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle3.7').onclick = toggle;

// Expand line 8 - toggle 4:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand8') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle4.7').onclick = toggle;




// ------- Expand line 9 ------- :
// Expand line 9 - toggle 1:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand9') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle1.8').onclick = toggle;

// Expand line 9 - toggle 2:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand9') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle2.8').onclick = toggle;

// Expand line 9 - toggle 3:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand9') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle3.8').onclick = toggle;

// Expand line 9 - toggle 4:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand9') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle4.8').onclick = toggle;




// ------- Expand line 10 ------- :
// Expand line 10 - toggle 1:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand10') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle1.9').onclick = toggle;

// Expand line 10 - toggle 2:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand10') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle2.9').onclick = toggle;

// Expand line 10 - toggle 3:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand10') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle3.9').onclick = toggle;

// Expand line 10 - toggle 4:

var divs = document.getElementsByTagName('div');

var toggle = function() {    
    for (var i = 0, l = divs.length; i < l; i++) {
        if (divs[i].getAttribute('class') == 'expand10') 
            if (divs[i].style.display == 'block') divs[i].style.display = '';
            else divs[i].style.display = 'block';
    }
}

document.getElementById('Toggle4.9').onclick = toggle;

